# Example Docker Compose for video-extractor services.
# Copy this to docker-compose.yaml and adjust as needed.
#
# Usage:
#   docker compose up -d
#
# Note: The Python ASR service is not containerized here. Run it directly with:
#   make run-asr
